const travixThemes = require('../travix-themes');
const themeBuilder = require('./lib/app');

const themes = travixThemes({ brand: 'cheaptickets', affiliate: 'NL' });
console.log(themes);

themeBuilder().build(themes).then(console.log)
