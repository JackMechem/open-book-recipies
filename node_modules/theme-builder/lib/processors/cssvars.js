'use strict';

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

var jsFlat = require('./jsflat');

function dictionaryToCssVars(obj) {
  return [':root {'].concat(_toConsumableArray(Object.keys(obj).map(function (key) {
    return '\t--' + key + ': ' + obj[key] + ';';
  })), ['}']).join('\n');
}

var CssVarsProcessor = {
  compile: function compile(obj, path) {
    return dictionaryToCssVars(jsFlat.compile(obj, path));
  }
};

module.exports = CssVarsProcessor;