'use strict';

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var js = require('./js');

function recursionCompile(obj, path) {
  return Object.keys(obj).reduce(function (result, key) {
    var keyAtPath = path ? path + '-' + key : key;
    return Object.assign(result, typeof obj[key] === 'string' ? _defineProperty({}, keyAtPath, obj[key]) : recursionCompile(obj[key], keyAtPath));
  }, {});
}

var JsFlatProcessor = {
  compile: function compile(obj, path) {
    return recursionCompile(js.compile(obj), path);
  }
};

module.exports = JsFlatProcessor;